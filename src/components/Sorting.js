import React from 'react';

const Sorting = (props) => {
    return (
        <div>
            {console.log('Sorting Log: ', props)}
            sorting..
        </div>
        )
}

export default Sorting;